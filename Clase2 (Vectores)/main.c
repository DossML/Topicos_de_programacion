#include "utiles.h"
#include "vectorEnteros.h"
#define TAM 10
int main()
{
    /* PRIMERA PARTE
    int a=2, b=10;
    intercambioEnt(&a, &b);
    printf("a %d, b %d", a, b);
    */
    int vec[TAM];
    int vec1[] = {1,2,5,9};
    int vec2[TAM]={7,5};
    mostrarVector(vec1, 4);

    return 0;
}
